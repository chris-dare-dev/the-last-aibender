{
  "name": "@aibender/testkit",
  "version": "0.0.0",
  "private": true,
  "description": "Synthesized fixture generators + fake servers for tests (plan §9.5). Fixtures are ALWAYS synthesized, never copied from real transcripts [X2]. M0 stub: JSONL transcript-line generator.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  }
}
