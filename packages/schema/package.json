{
  "name": "@aibender/schema",
  "version": "0.0.0",
  "private": true,
  "description": "SQLite migrations + typed row accessors for ALL harness ledgers. M0 stub: migration-runner interface only (tables land M1/M3/M4/M5 via ICR).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  }
}
