# pnpm behavior for the the-last-aibender workspace (monorepo scaffold, plan §2).
# Keep this file value-free: registry auth or tokens NEVER belong here [X2].
engine-strict=true
prefer-workspace-packages=true
