{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "SPIKE-D harness — extends the repo strict base; runs via tsx/vitest, no emit. Quarantined: not a workspace member.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
