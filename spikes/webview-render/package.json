{
  "name": "spike-a-webview-render",
  "version": "0.0.0",
  "private": true,
  "description": "M0 SPIKE-A (plan spikes i + iv): xterm 6 WebGL viability + navigator.gpu probe under a WKWebView proxy (Playwright WebKit). QUARANTINED - never imported by prod code.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "spike": "tsx src/run-spike.ts",
    "spike:webkit-install": "playwright install webkit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@xterm/addon-fit": "0.11.0",
    "@xterm/addon-webgl": "0.19.0",
    "@xterm/xterm": "6.0.0",
    "playwright": "1.61.1",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
